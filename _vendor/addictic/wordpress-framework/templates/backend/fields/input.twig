{% extends "backend/fields/_field.twig" %}
{% block field %}
    <input
            class="field"
            id="{{ name }}"
            name="{{ name }}"
            value="{{ value }}"
            {{ required ? "required" : "" }}
            {{ input }}
    >
{% endblock %}