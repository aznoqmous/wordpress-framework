{% extends "backend/fields/_field.twig" %}
{% block field %}
    <input
            class="field"
            id="{{ name }}"
            name="{{ name }}"
            type="text"
            value="{{ value }}"
            {{ required ? "required" : "" }}
    >
{% endblock %}